@page "/placeholder"

<PageTitle>Placeholder</PageTitle>

<div class="placeholder-background">
    <!-- TODO: Replace with actual background -->
</div>

<div class="placeholder-title-panel">
    <h1>Placeholder Screen</h1>
</div>

<div class="placeholder-message">
    <p>This screen is not yet implemented. Please check back later!</p>
</div>

<div class="placeholder-menu-buttons">
    <button class="menu-btn" @onclick="GoToMenu">Back to Menu</button>
</div>

@code {
    [Inject]
    private NavigationManager NavigationManager { get; set; } = default!;

    private void GoToMenu()
    {
        NavigationManager.NavigateTo("/");
    }
}

<style>
.placeholder-background {
    position: fixed;
    top: 0; left: 0; right: 0; bottom: 0;
    background: linear-gradient(120deg, #222233 0%, #111122 100%);
    z-index: 0;
    /* TODO: Add background visuals */
}
.placeholder-title-panel {
    margin-top: 60px;
    text-align: center;
    z-index: 1;
}
.placeholder-message {
    margin-top: 40px;
    text-align: center;
    color: #ccc;
    z-index: 1;
}
.placeholder-menu-buttons {
    display: flex;
    flex-direction: row;
    justify-content: center;
    margin-top: 40px;
    gap: 24px;
    z-index: 1;
}
.menu-btn {
    font-size: 1.2rem;
    padding: 12px 32px;
    border-radius: 8px;
    border: none;
    background: #222255;
    color: #fff;
    cursor: pointer;
    transition: background 0.2s;
}
.menu-btn:hover {
    background: #444488;
}
</style> 